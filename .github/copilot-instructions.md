# Workspace Instructions

- This is a Vite + React + TypeScript app.
- Keep form data persisted in `localStorage` and preserve the ability to reload older saved forms.
- Keep PDF export deterministic and based on form data, not screenshots.
- Prefer focused changes that keep the app fast to load and simple to maintain.